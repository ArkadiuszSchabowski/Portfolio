import { ScrollShowDirective } from './scroll-show.directive';

describe('ScrollShowDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollShowDirective();
    expect(directive).toBeTruthy();
  });
});
